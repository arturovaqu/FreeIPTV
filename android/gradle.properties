org.gradle.jvmargs=-Xmx1536M
android.useAndroidX=true
android.enableJetifier=false
# Fix cross-drive relative-path errors on Windows (project on D: vs pub-cache on C:).
# Kotlin incremental compilation uses File.relativeTo() which fails across drives.
kotlin.incremental=false
